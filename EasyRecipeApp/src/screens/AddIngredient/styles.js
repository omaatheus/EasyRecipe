import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        
    },
    viewInput: {
        minWidth: '100%',
        minHeight: 'auto',
        alignItems: 'center',
    },
    viewInput2: {
        marginTop: -100,
    },

})